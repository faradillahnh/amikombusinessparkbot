#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../abnf/bin/abnf_ast" "$@"
  ret=$?
else 
  node  "$basedir/../abnf/bin/abnf_ast" "$@"
  ret=$?
fi
exit $ret
